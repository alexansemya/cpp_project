#include "main.h"



int main(){
    return 0;
    //Creates Subject

//    std::vector<std::string> possible_answers1  = {"1. |x|", "2. x", "3. x^2"};
//    std::vector<std::string> possible_answers2 = {"1. 2x", "2. 0", "3. 2x + 1"};
//    std::vector<std::string> possible_answers3 = {"1. Нет", "2. Да", "3. Определить невозможно"};
//
//    Question question1("Выберите недифференцируюмую в нуле функцию", possible_answers1, 1);
//    Question question2("Посчитайте произвлдную функции f(x) = x^2 + x + 1", possible_answers2, 3);
//    Question question3("Существует ли последовательность, множеством частичных пределов которой является в точности множество Q?", possible_answers3, 2);
//
//    std::vector<Teacher> teachers;
//    std::vector<Student> students;
//    std::vector<Subject> subjects;
//    std::vector<Question> easy_question = {question2};
//    std::vector<Question> middle_question = {question1};
//    std::vector<Question> hard_question = {question3};
//    std::map<int, std::vector<Question>> question_list;
//    question_list.insert(std::pair<int, std::vector<Question>>(1, easy_question));
//    question_list.insert(std::pair<int, std::vector<Question>>(2, middle_question));
//    question_list.insert(std::pair<int, std::vector<Question>>(3, hard_question));
//    Subject subject("Математический анализ", teachers, question_list, 1);
//
//    //Starts the program
//
//    GeneralSystem general_system(students, teachers, subjects);
//    int option = EntrySystem::ChooseEntryOption();
//    int type = EntrySystem::ChooseEntryType();
//    if (option == 1 && type == 1) {Student::SignUp(general_system);}
//    if (option == 1 && type == 2) {Teacher::SignUp(general_system);}
//    if (option == 2 && type == 1) {Student::SignIn(general_system);}
//    if (option == 2 && type == 2) {Teacher::SignIn(general_system);}




}

